/* autogenerated by Processing revision 1293 on 2024-10-08 */
import processing.core.*;
import processing.data.*;
import processing.event.*;
import processing.opengl.*;

import hypermedia.net.*;
import processing.sound.*;

import java.util.HashMap;
import java.util.ArrayList;
import java.io.File;
import java.io.BufferedReader;
import java.io.PrintWriter;
import java.io.InputStream;
import java.io.OutputStream;
import java.io.IOException;

public class SoundTest extends PApplet {




int port = 9000;
int sendPort = 9001;
String ip = "127.0.0.1";
UDP udp;

JSONArray values;

ArrayList<SoundFile> soundFiles = new ArrayList<SoundFile>();

public void setup() {
  /* size commented out by preprocessor */;
  windowMove(-200, -200);

  udp = new UDP(this, port, ip);
  //udp.log(true);
  udp.listen(true);
  background(255, 0, 0);

  values = loadJSONArray("soundfiles.json");


  for (int i = 0; i < values.size(); i++) {
    JSONObject fileObject = values.getJSONObject(i);
    String file = fileObject.getString("file");
    float volume = fileObject.getFloat("volume");
    println(volume);
    println("load File: " + file);
    soundFiles.add(new SoundFile(this, file));
  }

  loop();
}

public void draw() {
  delay(100);
  udp.send("connected", ip, sendPort);
  background(0, 255, 0);
  for (int i = 0; i < soundFiles.size(); i++) {
    udp.send(i+":"+PApplet.parseInt(soundFiles.get(i).isPlaying()), ip, sendPort);
  }
}

public void receive(byte[] data, String ip, int port) {

  String message = new String(data);

  println(message);
  String[] splitted = message.split(":");
  if (splitted.length > 1) {
    String control = splitted[0];
    int index = PApplet.parseInt(splitted[1]);

    if (control.equals("play")) {
      if (soundFiles.get(index).isPlaying()) {
        soundFiles.get(index).stop();
        soundFiles.get(index).play();
      } else {
        soundFiles.get(index).play();
      }
    }

    if (control.equals("loop")) {
      soundFiles.get(index).loop();
    }

    if (control.equals("stop")) {
      soundFiles.get(index).stop();
    }
  }

  if (message.equals("hi")) {
    udp.send("connected", ip, sendPort);
  }
  if (message.equals("exit")) {
    exit();
  }
}


  public void settings() { size(100, 100); }

  static public void main(String[] passedArgs) {
    String[] appletArgs = new String[] { "SoundTest" };
    if (passedArgs != null) {
      PApplet.main(concat(appletArgs, passedArgs));
    } else {
      PApplet.main(appletArgs);
    }
  }
}
